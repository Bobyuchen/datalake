# source.yml
version: 2

sources:
  - name: postgresql
    database: postgresql
    schema: public
    tables:
      - name: auth_events

  - name: mongo
    database: mongo
    schema: demo
    tables:
      - name: listen_events
      - name: page_view_events